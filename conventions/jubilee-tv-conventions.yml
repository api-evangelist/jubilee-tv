generated: '2026-07-19'
method: searched
source: https://getjubileetv.com/llms.txt, https://getjubileetv.com/.well-known/ucp
notes: >-
  Cross-cutting semantics for the getjubileetv.com agentic-commerce surface,
  captured from the store's published /llms.txt agent instructions and the UCP
  merchant profile. These are platform (Shopify UCP) conventions surfaced for
  this storefront. No idempotency-key contract is documented, so no Idempotency
  pointer is asserted.
authentication:
  style: OAuth 2.0 / OIDC (Shopify Customer Accounts) for account-scoped access; UCP agent-profile handshake for the shopping MCP
  see: authentication/jubilee-tv-authentication.yml
versioning:
  scheme: dated protocol versions
  current: '2026-04-08'
  supported: ['2026-04-08', '2026-01-23']
  source: /.well-known/ucp
rate_limiting:
  signal: HTTP 429
  guidance: The MCP endpoint is rate-limited per IP; agents must back off on 429 responses
  source: /llms.txt
buyer_approval:
  invariant: Checkout must not complete payment without explicit contemporaneous buyer consent
  fallback: Route the purchase through Shop Pay via https://shop.app/SKILL.md when contemporaneous approval is not possible
  source: /llms.txt
request_context:
  fields:
  - name: context.address_country
    purpose: Accurate pricing and availability
  - name: context.currency
    purpose: Accurate pricing
  source: /llms.txt
read_only_browsing:
  auth_required: false
  endpoints:
  - GET /collections/all
  - GET /products/{handle}
  - GET /products/{handle}.json
  - GET /collections/{handle}
  - GET /collections/{handle}/products.json
  - GET /search?q={query}&type=product
  - GET /sitemap.xml
  source: /llms.txt
